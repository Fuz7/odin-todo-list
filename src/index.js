import './static/styles.css';
import {
  renderProjectSidebar,
  renderOverviewContent,
  renderProjectList,
} from './display';

renderProjectSidebar();
renderOverviewContent('Inbox');
renderProjectList();
